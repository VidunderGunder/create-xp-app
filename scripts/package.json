{
  "name": "@acme/scripts",
  "version": "0.1.0",
  "main": "index.ts",
  "scripts": {
    "syncroutes": "ts-node syncroutes.ts",
    "dev": "ts-node syncroutes-watcher.ts",
    "dev-legacy": "nodemon --watch '../packages/app/routes/**' --ext 'tsx' tsx --exec 'pnpm syncroutes'"
  },
  "dependencies": {
    "@acme/db": "0.1.0"
  },
  "devDependencies": {}
}
